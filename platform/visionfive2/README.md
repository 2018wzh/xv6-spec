# VisionFive 2 candidate boundary

This directory intentionally contains no board implementation yet. The hardware
runner fails closed until an operator supplies the physical board identity,
firmware, DTB, image, serial endpoint, and workload. QEMU and fixture output do
not satisfy that boundary, and automated evidence remains
`pending_human_review`.
