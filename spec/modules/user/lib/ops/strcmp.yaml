id: user/lib.strcmp

module: user/lib

operation: strcmp
stage: full-syscall
purpose: >
  Compare strings p and q lexicographically. Returns 0 if equal, negative if p<q, positive if p>q.

related_slice: slice-09-full-syscall
related_adr: null
depends_on:
  requires_modules:
    - user/lib
  requires_ops:
    []

rely:
  state_assumptions:
  callable_interfaces:
  resource_assumptions:
    - sufficient resources available
  lock_assumptions:
    - none

guarantee:
  returns:
    - int: comparison result
  state_updates: []
  side_effects:
    - none
  emitted_events: []
preconditions:
  []
postconditions:
  []
invariants_preserved:
  []
failure_semantics:
  - none

concurrency:
  atomicity: user-space only
  lock_order:
    []
  interrupt_state: enabled (user mode)
  wait_wakeup_rules:
    []

security:
  authority_check: none
  isolation_boundary: user-space only
  user_pointer_policy: none

observability:
  traces: []
  counters: []
  expected_logs: []

test_obligations:
  public:
    - strcmp_basic
  generated: []
  hidden_tags:
    []

codegen:
  targets:
    - kind: symbol
      path: user/ulib.c
      symbols: [strcmp]
      owner: user/lib
      mode: create
  forbidden_changes:
    - do not modify code outside the marked region
    - do not create helper files
    - do not modify build scripts
  required_followup_checks:
    - make build
