id: kernel/sysfile.sys_write

module: kernel/sysfile

operation: sys_write
stage: full-syscall
purpose: >
  Write up to n bytes from a user buffer to a file descriptor. Validates fd and calls filewrite.

related_slice: slice-09-full-syscall
related_adr: null
depends_on:
  requires_modules:
    - kernel/sysfile
  requires_ops:
    - kernel/sysfile.argfd
    - kernel/file.filewrite

rely:
  state_assumptions:
  callable_interfaces:
    - argfd (internal)
    - argint/argaddr (from kernel/syscall)
    - filewrite (from kernel/file)
  resource_assumptions:
    - sufficient resources available
  lock_assumptions:
    - none

guarantee:
  returns:
    - int: number of bytes written, or -1 on error
  state_updates: []
  side_effects:
    - data written to file
  emitted_events: []
preconditions:
  - fd is valid
postconditions:
  - data is on the output device or in the file
invariants_preserved:
  []
failure_semantics:
  - returns -1 on invalid fd or write error

concurrency:
  atomicity: single operation
  lock_order:
    []
  interrupt_state: enabled
  wait_wakeup_rules:
    []

security:
  authority_check: none
  isolation_boundary: kernel-space only
  user_pointer_policy: none

observability:
  traces: []
  counters: []
  expected_logs: []

test_obligations:
  public:
    - sys_write_basic
  generated: []
  hidden_tags:
    []

codegen:
  targets:
    - kind: symbol
      path: kernel/sysfile.c
      symbols: [sys_write]
      owner: kernel/sysfile
      mode: create
  forbidden_changes:
    - do not modify code outside the marked region
    - do not create helper files
    - do not modify build scripts
  required_followup_checks:
    - make build
