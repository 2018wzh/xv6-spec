id: kernel/printk.printkinit

module: kernel/printk

operation: printkinit
stage: device
purpose: >
  Initialize the printk lock (pr.lock). Called once during boot.

related_slice: slice-08-device
related_adr: null
depends_on:
  requires_modules:
    - kernel/printk
  requires_ops:
    []

rely:
  state_assumptions:
  callable_interfaces:
    - initlock (from kernel/lock)
  resource_assumptions:
    - sufficient resources available
  lock_assumptions:
    - none

guarantee:
  returns:
    - void
  state_updates: []
  side_effects:
    - pr.lock initialized
  emitted_events: []
preconditions:
  - called once during boot
postconditions:
  - printk is ready
invariants_preserved:
  []
failure_semantics:
  - none

concurrency:
  atomicity: single operation
  lock_order:
    []
  interrupt_state: enabled
  wait_wakeup_rules:
    []

security:
  authority_check: none
  isolation_boundary: kernel-space only
  user_pointer_policy: none

observability:
  traces: []
  counters: []
  expected_logs: []

test_obligations:
  public:
    - printkinit_basic
  generated: []
  hidden_tags:
    []

codegen:
  targets:
    - kind: symbol
      path: kernel/printk.c
      symbols: [printkinit]
      owner: kernel/printk
      mode: create
  forbidden_changes:
    - do not modify code outside the marked region
    - do not create helper files
    - do not modify build scripts
  required_followup_checks:
    - make build
