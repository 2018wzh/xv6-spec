id: kernel/fs.iunlock

module: kernel/fs

operation: iunlock
stage: filesystem
purpose: >
  Unlock an inode. Release the inode's sleeplock.

related_slice: slice-06-filesystem
related_adr: null
depends_on:
  requires_modules:
    - kernel/fs
  requires_ops:
    []

rely:
  state_assumptions:
    - file system is initialized
  callable_interfaces:
    - bread/bwrite/brelse (from kernel/bio)
    - begin_op/end_op/log_write (from kernel/log)
  resource_assumptions:
    - sufficient resources available
  lock_assumptions:
    - per-inode sleeplock for inode operations
    - log.lock for transaction boundaries

guarantee:
  returns:
    - void
  state_updates: []
  side_effects:
    - inode unlocked
  emitted_events: []

preconditions:
  []
postconditions:
  []
invariants_preserved:
  []
failure_semantics:
  - none

concurrency:
  atomicity: single operation
  lock_order:
    []
  interrupt_state: enabled
  wait_wakeup_rules:
    []

security:
  authority_check: none
  isolation_boundary: kernel-space only
  user_pointer_policy: none

observability:
  traces: []
  counters: []
  expected_logs: []

test_obligations:
  public:
    - iunlock_basic
  generated: []
  hidden_tags:
    []

codegen:
  targets:
    - kind: symbol
      path: kernel/fs.c
      symbols: [iunlock]
      owner: kernel/fs
      mode: create
  forbidden_changes:
    - do not modify code outside the marked region
    - do not create helper files
    - do not modify build scripts
  required_followup_checks:
    - make build
