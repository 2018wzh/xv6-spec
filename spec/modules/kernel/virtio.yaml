id: kernel/virtio
module: kernel/virtio
level: 2
purpose: QEMU block transport.
owns:
- kernel/virtio_disk.c
interface: []
properties:
- id: kernel-virtio-deterministic
  text: behavior is deterministic under the published lab checks
errors:
- invalid state fails explicitly
