id: kernel/string.memcpy

module: kernel/string

operation: memcpy
stage: boot
purpose: >
  Copy n bytes from src to dst. Delegates to memmove for safety with overlapping regions.

related_slice: slice-01-boot
related_adr: null
depends_on:
  requires_modules:
    - kernel/string
  requires_ops:
    []

rely:
  state_assumptions:
  callable_interfaces:
    - memmove (internal)
  resource_assumptions:
    - sufficient resources available
  lock_assumptions:
    - none

guarantee:
  returns:
    - void* (dst)
  state_updates: []
  side_effects:
    - n bytes copied
  emitted_events: []
preconditions:
  []
postconditions:
  - same as memmove
invariants_preserved:
  []
failure_semantics:
  - none

concurrency:
  atomicity: single operation
  lock_order:
    []
  interrupt_state: enabled
  wait_wakeup_rules:
    []

security:
  authority_check: none
  isolation_boundary: kernel-space only
  user_pointer_policy: none

observability:
  traces: []
  counters: []
  expected_logs: []

test_obligations:
  public:
    - memcpy_basic
  generated: []
  hidden_tags:
    []

codegen:
  targets:
    - kind: symbol
      path: kernel/string.c
      symbols: [memcpy]
      owner: kernel/string
      mode: create
  forbidden_changes:
    - do not modify code outside the marked region
    - do not create helper files
    - do not modify build scripts
  required_followup_checks:
    - make build
