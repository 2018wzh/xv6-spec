id: kernel/string.memset

module: kernel/string

operation: memset
stage: boot
purpose: >
  Fill the first n bytes of dst with byte c. Returns dst.

related_slice: slice-01-boot
related_adr: null
depends_on:
  requires_modules:
    - kernel/string
  requires_ops:
    []

rely:
  state_assumptions:
  callable_interfaces:
  resource_assumptions:
    - sufficient resources available
  lock_assumptions:
    - none

guarantee:
  returns:
    - void* (dst)
  state_updates: []
  side_effects:
    - first n bytes of dst set to c
  emitted_events: []
preconditions:
  []
postconditions:
  - dst[0..n-1] == c
invariants_preserved:
  []
failure_semantics:
  - none

concurrency:
  atomicity: single operation
  lock_order:
    []
  interrupt_state: enabled
  wait_wakeup_rules:
    []

security:
  authority_check: none
  isolation_boundary: kernel-space only
  user_pointer_policy: none

observability:
  traces: []
  counters: []
  expected_logs: []

test_obligations:
  public:
    - memset_basic
  generated: []
  hidden_tags:
    []

codegen:
  targets:
    - kind: symbol
      path: kernel/string.c
      symbols: [memset]
      owner: kernel/string
      mode: create
  forbidden_changes:
    - do not modify code outside the marked region
    - do not create helper files
    - do not modify build scripts
  required_followup_checks:
    - make build
