system:
  name: xv6-spec
  language: C
  isa: RISC-V RV64GC
machine:
  qemu:
    model: virt
  hardware: {}
kernel:
  organization: monolithic teaching kernel
  execution: supervisor-mode kernel with deterministic interrupt handling
  protection: Sv39 page tables isolate mapped regions
  communication: traps cross the user and kernel boundary
  resource_model: explicit ownership for resources introduced through this lab
required_mechanisms:
- deterministic early entry
- virtual memory
- traps
- process scheduling
- system calls
- filesystem
- file descriptions
- pipes
- command shell
composition_invariants:
- every published check is deterministic and scoped to the current lab
non_goals:
- mechanisms assigned to later labs
hardware_port:
  board: not in current lab scope
  boot: not in current lab scope
  console: not in current lab scope
  interrupt: not in current lab scope
