id: interface/kernel-plic
name: external interrupt boundary
boundary: abi
module: kernel/trap
operations:
- name: current_lab_contract
  pre: []
  post:
  - the current lab boundary is preserved
  errors:
  - invalid state fails explicitly
  properties: []
