id: interface/trap-frame
name: user register handoff
boundary: abi
module: kernel/trap
operations:
- name: current_lab_contract
  pre: []
  post:
  - the current lab boundary is preserved
  errors:
  - invalid state fails explicitly
  properties: []
