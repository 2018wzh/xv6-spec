id: lab5-process-syscall
reason: Lab 5 extends traps and memory ownership across the new process and syscall boundary.
changes:
- kernel/memory
- kernel/trap
- kernel/process
- kernel/syscall
new_invariants:
- User pointers are translated through the owning process page table.
- Trap-frame state is the sole register handoff between user and kernel execution.
